Sample cases, for testing the renderer only. They sit in this subfolder because build-cases.py
only globs the parent directory, so nothing in here can ever be rendered into the site by accident.
Copy one out, rename it, and fill it with real data.
